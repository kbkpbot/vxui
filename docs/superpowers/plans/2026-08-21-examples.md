# vxui 四个新示例实施计划（system-monitor / chat / run-js-playground / data-table）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按 spec 新增 4 个示例，补齐 broadcast/run_js/事件钩子/服务端表格四大能力演示。

**Architecture:** 每个示例独立目录（`examples/<name>/`），纯逻辑抽为可测函数配 `_test.v`，
UI 为暗色 htmx 页面；后端推送统一走 `broadcast(json({"cmd":"oob_update",...}))`
（裸 HTML 会被前端当无效 RPC 丢弃）。

**Tech Stack:** V + vxui + htmx 2.x + 手写 CSS；模板用 V 内置 `$tmpl`。

## Global Constraints

- 零第三方前端库；图表一律内联 SVG
- 每个示例：`README.md` + `ui/index.html` + `ui/js/{htmx.js,vxui-ws.js}` 符号链接
  （`ln -s ../../../../js/htmx.js examples/<name>/ui/js/htmx.js`，同 vxui-ws）
- 编译门禁：在示例目录内 `v -o /dev/null main.v` 退出码 0
- 测试门禁：`v test .`（仓库根）全绿
- 不修改框架源码（发现 bug 单独报告）
- 提交信息用 conventional commits（仓库既有风格），每个 Task 至少一个提交
- 后端推送格式固定为：
  `json2.encode({'cmd': 'oob_update', 'html': fragment})` 经 `app.broadcast(...)` 发送

---

### Task 1: system-monitor — 统计解析纯函数（TDD）

**Files:**
- Create: `examples/system-monitor/main.v`（仅含本任务需要的纯函数与类型）
- Test: `examples/system-monitor/monitor_test.v`

**Interfaces:**
- Produces（Task 2 复用）:
  - `struct CpuSnapshot { pub: idle u64 total u64 }`
  - `parse_cpu_line(line string) ?CpuSnapshot`
  - `cpu_percent(prev CpuSnapshot, cur CpuSnapshot) f64`
  - `struct MemInfo { pub: total_kb u64 avail_kb u64 }`
  - `parse_meminfo(content string) MemInfo`
  - `mem_percent(m MemInfo) f64`
  - `read_loadavg() f64`

- [ ] **Step 1: 写失败测试**

创建 `examples/system-monitor/monitor_test.v`：

```v
module main

fn test_parse_cpu_line() {
	snap := parse_cpu_line('cpu  100 0 100 700 100 0 0 0 0 0') or { panic('should parse') }
	assert snap.idle == 800 // idle 700 + iowait 100
	assert snap.total == 1000
	assert parse_cpu_line('garbage') is none
}

fn test_cpu_percent() {
	prev := CpuSnapshot{idle: 800, total: 1000}
	cur := CpuSnapshot{idle: 900, total: 1200}
	// busy delta = (1200-900)-(900-800) = 300-100 = 200 ; total delta = 200
	pct := cpu_percent(prev, cur)
	assert pct > 99.9 && pct <= 100.0
	cur2 := CpuSnapshot{idle: 1100, total: 1200}
	assert cpu_percent(prev, cur2) < 0.001
	// zero total delta -> -1 sentinel
	assert cpu_percent(cur, cur) == -1.0
}

fn test_parse_meminfo() {
	content := 'MemTotal:       16090520 kB\nMemFree:         1024 kB\nMemAvailable:    8045260 kB\nSwapTotal:             0 kB\n'
	mi := parse_meminfo(content)
	assert mi.total_kb == 16090520
	assert mi.avail_kb == 8045260
	// half available -> 50%
	assert (mem_percent(mi) - 50.0).abs() < 0.01
}

fn test_meminfo_missing_fields_is_zero_safe() {
	mi := parse_meminfo('')
	assert mem_percent(mi) == 0.0
}
```

- [ ] **Step 2: 运行确认失败**

Run: `cd examples/system-monitor && v test .`
Expected: FAIL（`parse_cpu_line` 等未定义）

- [ ] **Step 3: 最小实现**

创建 `examples/system-monitor/main.v`：

```v
module main

import os

// Demonstrates: multi_client, broadcast, oob_update, get_client_count
// Backend samples CPU/RAM/load once per second and broadcasts OOB updates
// to every connected browser window.

const default_page_html_file = './ui/index.html'

// circumference of the SVG gauge rings (2 * pi * r, r = 52)
const ring_circumference = 326.73

struct CpuSnapshot {
pub:
	idle  u64
	total u64
}

struct MemInfo {
pub:
	total_kb u64
	avail_kb u64
}

fn parse_cpu_line(line string) ?CpuSnapshot {
	fields := line.split(' ').filter(it.len > 0)
	if fields.len < 5 || fields[0] != 'cpu' {
		return none
	}
	mut total := u64(0)
	for i := 1; i < fields.len; i++ {
		total += fields[i].u64()
	}
	idle := fields[4].u64() + fields[5].u64() // idle + iowait
	return CpuSnapshot{
		idle:  idle
		total: total
	}
}

fn cpu_percent(prev CpuSnapshot, cur CpuSnapshot) f64 {
	d_total := cur.total - prev.total
	if d_total == 0 {
		return -1.0
	}
	d_idle := cur.idle - prev.idle
	return f64(d_total-d_idle) / f64(d_total) * 100.0
}

fn parse_meminfo(content string) MemInfo {
	mut mi := MemInfo{}
	for line in content.split('\n') {
		if line.starts_with('MemTotal:') {
			mi.total_kb = field_kb(line)
		} else if line.starts_with('MemAvailable:') {
			mi.avail_kb = field_kb(line)
		}
	}
	return mi
}

// field_kb extracts the numeric kB value from a "/proc/meminfo" style line
fn field_kb(line string) u64 {
	for part in line.all_after(':').split(' ') {
		if part.len > 0 {
			return part.u64()
		}
	}
	return 0
}

fn mem_percent(m MemInfo) f64 {
	if m.total_kb == 0 || m.avail_kb >= m.total_kb {
		return 0.0
	}
	return f64(m.total_kb-m.avail_kb) / f64(m.total_kb) * 100.0
}

fn read_loadavg() f64 {
	content := os.read_file('/proc/loadavg') or { return 0.0 }
	return content.all_before(' ').f64()
}
```

- [ ] **Step 4: 运行确认通过**

Run: `cd examples/system-monitor && v test .`
Expected: PASS（4 个测试）

- [ ] **Step 5: 提交**

```bash
git add examples/system-monitor/
git commit -m "feat: add system-monitor stats parsing functions"
```

---

### Task 2: system-monitor — 采样循环与广播接线

**Files:**
- Modify: `examples/system-monitor/main.v`（追加 App、采样、渲染片段代码）

**Interfaces:**
- Consumes: Task 1 全部函数与常量 `ring_circumference`
- Produces: 可编译的完整后端；`render_dashboard(app &App) string`（Task 3 的 index.html
  初始加载不依赖它——首屏由浏览器本地占位，随后被首个广播帧覆盖）

- [ ] **Step 1: 在 main.v 追加以下代码**

```v
import vxui
import json2 as vxjson
import time
import math

struct App {
	vxui.Context
mut:
	history     []f64
	has_proc    bool
	last_sample f64 // unix ms of previous cpu sample
	prev        CpuSnapshot
	mem         MemInfo
	load        f64
	cpu         f64
}

// start_sampler runs in its own goroutine; samples once per second and
// broadcasts an oob_update command with freshly rendered dashboard parts.
fn start_sampler(mut app App) {
	time.sleep(1500 * time.millisecond) // let the WS server come up first
	app.has_proc = os.exists('/proc/stat')
	mut snap := read_cpu_snapshot()
	if s := snap {
		app.prev = s
	}
	for {
		app.sample_once()
		fragment := render_dashboard(app)
		payload := {'cmd': 'oob_update', 'html': fragment}
		app.broadcast(vxjson.encode(payload)) or {}
		time.sleep(1 * time.second)
	}
}

// read_cpu_snapshot wraps parse_cpu_line over the live /proc/stat head line
fn read_cpu_snapshot() ?CpuSnapshot {
	lines := os.read_lines('/proc/stat') or { return none }
	if lines.len == 0 {
		return none
	}
	return parse_cpu_line(lines[0])
}

fn (mut app App) sample_once() {
	app.load = read_loadavg()
	content := os.read_file('/proc/meminfo') or { '' }
	app.mem = parse_meminfo(content)

	cur := read_cpu_snapshot()
	if s := cur {
		pct := cpu_percent(app.prev, s)
		if pct >= 0 {
			app.cpu = pct
			app.history << pct
			if app.history.len > 60 {
				app.history.delete(0)
			}
		}
		app.prev = s
	} else {
		// simulated fallback (non-Linux): smooth sine wave around 40%
		t := time.now().unix() % 3600
		app.cpu = 40.0 + 30.0 * math.sin(f64(t) / 10.0)
		app.history << app.cpu
		if app.history.len > 60 {
			app.history.delete(0)
		}
	}
}

// gauge_svg renders one ring gauge as an OOB-swappable div
fn gauge_svg(id string, label string, pct f64, color string) string {
	clamped := math.min(math.max(pct, 0.0), 100.0)
	offset := ring_circumference * (1.0 - clamped / 100.0)
	return '<div id="gauge-${id}" hx-swap-oob="true" class="gauge">
		<svg viewBox="0 0 120 120">
			<circle class="ring-bg" cx="60" cy="60" r="52"/>
			<circle class="ring-fg" cx="60" cy="60" r="52" style="stroke:${color};stroke-dashoffset:${offset:.1f}"/>
		</svg>
		<div class="gauge-value">${clamped:.1f}%</div>
		<div class="gauge-label">${label}</div>
	</div>'
}

// history_svg renders the CPU% sparkline polyline
fn history_svg(history []f64) string {
	mut pts := ''
	w := 600.0
	h := 120.0
	step := if history.len > 1 { w / f64(history.len - 1) } else { w }
	for i, v in history {
		x := step * f64(i)
		y := h - h * v / 100.0
		pts += '${x:.1f},${y:.1f} '
	}
	label := if history.len > 0 { '${history.last():.1f}%' } else { '--' }
	return '<div id="history" hx-swap-oob="true" class="history">
		<div class="history-title">CPU 历史 <span>${label}</span></div>
		<svg viewBox="0 0 ${w:.0f} ${h:.0f}" preserveAspectRatio="none">
			<polyline fill="none" stroke="#38ef7d" stroke-width="2" points="${pts}"/>
		</svg>
	</div>'
}

fn render_dashboard(app &App) string {
	mut out := gauge_svg('cpu', 'CPU', app.cpu, '#38ef7d')
	out += gauge_svg('mem', '内存', mem_percent(app.mem), '#4fc3f7')
	out += gauge_svg('load', '负载×100', app.load * 100.0, '#ffb74d')
	out += history_svg(app.history)
	out += '<div id="clients" hx-swap-oob="true" class="clients">在线窗口：${app.get_client_count()}</div>'
	return out
}

fn main() {
	mut app := App{}

	app.config.app_name = 'system-monitor'
	app.config.multi_client = true
	app.config.close_timer_ms = 60000

	spawn start_sampler(mut app)

	vxui.run(mut app, default_page_html_file)!
}
```

注意：`import json2 as vxjson` 若编译器报模块名不符（应为 `x.json2`），改为
`import x.json2 as vxjson`。以编译通过为准。

- [ ] **Step 2: 编译验证**

Run: `cd examples/system-monitor && v -o /dev/null main.v`
Expected: 退出码 0。若闭包/spawn 报可变别名错误，把 `spawn start_sampler(mut app)`
改为先 `mut sampler_app := &app` 再 `spawn start_sampler(mut sampler_app)`。

- [ ] **Step 3: 提交**

```bash
git add examples/system-monitor/main.v
git commit -m "feat: system-monitor sampling loop with oob_update broadcast"
```

---

### Task 3: system-monitor — 前端页面与 README

**Files:**
- Create: `examples/system-monitor/ui/index.html`
- Create: `examples/system-monitor/ui/stylesheet.css`
- Create: `examples/system-monitor/ui/js/htmx.js`（符号链接）
- Create: `examples/system-monitor/ui/js/vxui-ws.js`（符号链接）
- Create: `examples/system-monitor/README.md`

- [ ] **Step 1: 建立目录与符号链接**

```bash
mkdir -p examples/system-monitor/ui/js
cd examples/system-monitor/ui/js
ln -s ../../../../js/htmx.js htmx.js
ln -s ../../../../js/vxui-ws.js vxui-ws.js
```

- [ ] **Step 2: 写 ui/index.html**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>vxui System Monitor</title>
  <link rel="stylesheet" href="./stylesheet.css"/>
  <script src="./js/htmx.js"></script>
  <script src="./js/vxui-ws.js"></script>
</head>
<body hx-ext="vxui-ws">
  <header>
    <h1>系统监控</h1>
    <span id="demo-badge" class="badge hidden">demo data</span>
    <div id="clients" class="clients">在线窗口：--</div>
  </header>

  <section class="gauges">
    <!-- 占位：首帧广播后即被真实仪表替换 -->
    <div id="gauge-cpu" class="gauge"><div class="gauge-label">等待数据…</div></div>
    <div id="gauge-mem" class="gauge"><div class="gauge-label">等待数据…</div></div>
    <div id="gauge-load" class="gauge"><div class="gauge-label">等待数据…</div></div>
  </section>

  <section id="history" class="history">
    <div class="history-title">CPU 历史 <span>--</span></div>
  </section>
</body>
</html>
```

- [ ] **Step 3: 写 ui/stylesheet.css**

```css
* { box-sizing: border-box; margin: 0; padding: 0; }
body {
	background: #12161f;
	color: #dfe6ee;
	font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
	min-height: 100vh;
	padding: 24px;
}
header { display: flex; align-items: center; gap: 16px; margin-bottom: 24px; }
header h1 { font-size: 22px; font-weight: 600; }
.badge {
	background: #eb3349; color: #fff; border-radius: 4px;
	font-size: 12px; padding: 2px 8px;
}
.hidden { display: none; }
.clients { margin-left: auto; color: #8b98a9; font-size: 14px; }

.gauges { display: flex; gap: 32px; justify-content: center; margin: 24px 0; }
.gauge { position: relative; width: 180px; height: 180px; }
.gauge svg { width: 100%; height: 100%; transform: rotate(-90deg); }
.ring-bg { fill: none; stroke: #232b38; stroke-width: 10;
	stroke-dasharray: 326.73; stroke-dashoffset: 0; }
.ring-fg { fill: none; stroke-width: 10; stroke-linecap: round;
	stroke-dasharray: 326.73; transition: stroke-dashoffset .6s ease; }
.gauge-value {
	position: absolute; top: 44%; left: 0; width: 100%;
	text-align: center; font-size: 26px; font-weight: 700;
}
.gauge-label { position: absolute; top: 62%; left: 0;
	width: 100%; text-align: center; color: #8b98a9; font-size: 13px; }

.history { background: #181f2b; border-radius: 10px; padding: 16px; max-width: 640px;
	margin: 0 auto; }
.history-title { display: flex; justify-content: space-between;
	margin-bottom: 8px; color: #8b98a9; font-size: 14px; }
.history-title span { color: #38ef7d; }
.history svg { width: 100%; height: 120px; background: #12161f; border-radius: 6px; }
```

- [ ] **Step 4: 写 README.md**

```markdown
# system-monitor 实时系统监控仪表盘

后端每秒采集 CPU/内存/负载并广播到所有窗口，多开几个浏览器窗口即可看到同步刷新。

## 演示 API

| API | 用途 |
|-----|------|
| `multi_client` | 允许多个浏览器窗口同时连接 |
| `broadcast` | 向全部客户端推送 |
| `oob_update` 命令 | 推送 OOB HTML 片段 |
| `get_client_count` | 显示在线窗口数 |

## 运行

​```bash
v run main.v
​```

Linux 下展示 `/proc/stat`、`/proc/meminfo`、`/proc/loadavg` 真实数据；
其他平台自动降级为模拟数据（页面右上角显示 demo data 徽标，需手动 F12 移除 hidden 类查看）。
```

（注：demo 徽标当前未联动，README 如实描述或删去该句——实现时以实际行为为准修正文案。）

- [ ] **Step 5: 冒烟验证**

Run: `cd examples/system-monitor && timeout 8 v run main.v; true`
Expected: 无编译错误，日志出现 "Browser started"。人工验收项（记录于 PR 描述）：
两个窗口数值一致、环形图动画、折线增长。

- [ ] **Step 6: 提交**

```bash
git add examples/system-monitor/
git commit -m "feat: system-monitor dashboard frontend"
```

---

### Task 4: chat — 多窗口聊天室（完整示例）

**Files:**
- Create: `examples/chat/main.v`
- Create: `examples/chat/ui/index.html`
- Create: `examples/chat/ui/stylesheet.css`
- Create: `examples/chat/ui/js/{htmx.js,vxui-ws.js}`（符号链接，同 Task 3 Step 1）
- Create: `examples/chat/README.md`

**关键机制**：框架不在请求消息中携带 client_id，页面脚本监听 `vxui:authenticated`
事件把 `window.vxuiWs.getClientId()` 写入隐藏输入，随表单一起提交。

- [ ] **Step 1: 写 main.v**

```v
module main

import vxui
import sync
import x.json2

// Demonstrates: multi_client, broadcast, get_clients, on_event lifecycle hooks

const default_page_html_file = './ui/index.html'

struct App {
	vxui.Context
mut:
	mu        sync.RwMutex
	nicknames map[string]string
}

// msg_param pulls a string out of the request parameters object
fn msg_param(message map[string]json2.Any, key string) string {
	if params := message['parameters'] {
		m := params.as_map()
		if v := m[key] {
			return v.str()
		}
	}
	return ''
}

// escape_html escapes user supplied text before embedding into HTML
fn esc(s string) string {
	return s.replace_each(['&', '&amp;', '<', '&lt;', '>', '&gt;', '"', '&quot;', "'", '&#x27;'])
}

// bubble renders one chat bubble element
fn bubble(kind string, text string) string {
	return '<div class="msg ${kind}">${text}</div>'
}

// users_fragment renders the OOB online-user panel
fn (mut app App) users_fragment() string {
	app.mu.rlock()
	defer {
		app.mu.unlock()
	}
	mut lis := ''
	for cid, nick in app.nicknames {
		lis += '<li data-cid="${cid}">${esc(nick)}</li>'
	}
	count := app.nicknames.len
	return '<ul id="user-list" hx-swap-oob="true">${lis}<li class="count">${count} 人在线</li></ul>'
}

// system_broadcast composes a system line plus refreshed user list and sends
// it to every connected window (including the originator).
fn (mut app App) system_broadcast(html string) {
	payload := {'cmd': 'oob_update', 'html': html + app.users_fragment()}
	app.broadcast(json2.encode(payload)) or {}
}

@['/join']
fn (mut app App) join(message map[string]json2.Any) string {
	nick := esc(msg_param(message, 'nick'))
	cid := esc(msg_param(message, 'cid'))
	if nick == '' || cid == '' {
		return ''
	}
	app.mu.lock()
	app.nicknames[cid] = nick
	app.mu.unlock()
	app.system_broadcast(bubble('system', '${nick} 加入了聊天室'))
	return ''
}

@['/send']
fn (mut app App) send(message map[string]json2.Any) string {
	text := esc(msg_param(message, 'text'))
	cid := esc(msg_param(message, 'cid'))
	if text == '' || cid == '' {
		return ''
	}
	app.mu.rlock()
	nick := app.nicknames[cid] or { '匿名' }
	app.mu.runlock()
	app.system_broadcast(bubble('chat', '<b>${nick}：</b>${text}'))
	return ''
}

fn main() {
	mut app := App{}
	app.nicknames = map[string]string{}
	app.config.app_name = 'chat-room'
	app.config.multi_client = true
	app.config.close_timer_ms = 60000

	app.on_event(.client_disconnected, fn [mut app] (e vxui.EventData) {
		if e.client_id == '' {
			return
		}
		app.mu.lock()
		nick := app.nicknames[e.client_id] or {
			app.mu.unlock()
			return
		}
		app.nicknames.delete(e.client_id)
		app.mu.unlock()
		app.system_broadcast(bubble('system', '${esc(nick)} 离开了聊天室'))
	})

	vxui.run(mut app, default_page_html_file)!
}
```

注意：若 `fn [mut app]` 事件闭包编译报错（可变别名），改为注册前创建指针副本：
`mut ap := &app` 后使用 `fn [ap]`。

- [ ] **Step 2: 写 ui/index.html**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>vxui Chat</title>
  <link rel="stylesheet" href="./stylesheet.css"/>
  <script src="./js/htmx.js"></script>
  <script src="./js/vxui-ws.js"></script>
</head>
<body hx-ext="vxui-ws">
  <input type="hidden" id="cid" name="cid" value="">
  <aside>
    <h2>在线用户</h2>
    <ul id="user-list"><li class="count">0 人在线</li></ul>
  </aside>
  <main>
    <div id="messages"></div>
    <form id="join-form" hx-post="/join" hx-swap="none">
      <input name="nick" placeholder="输入昵称加入" maxlength="20" required>
      <button>加入</button>
    </form>
    <form id="chat-form" class="hidden" hx-post="/send" hx-swap="none">
      <input name="text" placeholder="说点什么…" maxlength="200" autocomplete="off">
      <button>发送</button>
    </form>
  </main>
  <script>
    document.body.addEventListener('vxui:authenticated', function () {
      setInterval(function () {
        var c = window.vxuiWs && window.vxuiWs.getClientId()
        if (c) { document.getElementById('cid').value = c }
      }, 1000)
    })
    document.getElementById('join-form').addEventListener('htmx:afterRequest', function () {
      this.classList.add('hidden')
      document.getElementById('chat-form').classList.remove('hidden')
    })
  </script>
</body>
</html>
```

- [ ] **Step 3: 写 ui/stylesheet.css**

```css
* { box-sizing: border-box; margin: 0; padding: 0; }
body { display: flex; height: 100vh; background: #12161f; color: #dfe6ee;
	font-family: -apple-system, 'Segoe UI', sans-serif; }
aside { width: 220px; background: #181f2b; padding: 16px; }
aside h2 { font-size: 15px; color: #8b98a9; margin-bottom: 12px; }
#user-list { list-style: none; }
#user-list li { padding: 6px 8px; border-radius: 6px; }
#user-list li:hover { background: #232b38; }
#user-list .count { color: #8b98a9; font-size: 12px; margin-top: 8px; }
main { flex: 1; display: flex; flex-direction: column; padding: 16px; }
#messages { flex: 1; overflow-y: auto; display: flex; flex-direction: column; gap: 8px; }
.msg { padding: 8px 12px; border-radius: 8px; background: #1c2532; max-width: 70%; }
.msg.system { align-self: center; background: transparent; color: #8b98a9; font-size: 13px; }
.msg b { color: #4fc3f7; }
form { display: flex; gap: 8px; margin-top: 12px; }
form input { flex: 1; background: #1c2532; border: 1px solid #2a3444; color: #dfe6ee;
	border-radius: 6px; padding: 10px 12px; }
form button { background: #11998e; border: 0; color: #fff; border-radius: 6px;
	padding: 10px 18px; cursor: pointer; }
.hidden { display: none; }
```

- [ ] **Step 4: 写 README.md**

```markdown
# chat 多窗口聊天室

开多个窗口、分别取昵称加入即可互聊；关闭窗口时其余窗口收到离开提示。

## 演示 API

| API | 用途 |
|-----|------|
| `multi_client` / `broadcast` | 消息扇出到所有窗口 |
| `on_event(.client_disconnected)` | 断连清理昵称并广播离开消息 |
| `get_clients` | 在线列表基础 |

## 实现要点

框架请求中不含 client_id，页面监听 `vxui:authenticated` 事件读取
`window.vxuiWs.getClientId()` 写入隐藏域随表单提交。

## 运行

​```bash
v run main.v
​```
```

- [ ] **Step 5: 编译 + 冒烟**

Run: `cd examples/chat && v -o /dev/null main.v && timeout 8 v run main.v; true`
Expected: 编译通过、正常启动。

- [ ] **Step 6: 提交**

```bash
git add examples/chat/
git commit -m "feat: multi-window chat example"
```

---

### Task 5: run-js-playground — 后端驱动前端（完整示例）

**Files:**
- Create: `examples/run-js-playground/main.v`
- Create: `examples/run-js-playground/ui/index.html`
- Create: `examples/run-js-playground/ui/stylesheet.css`
- Create: `examples/run-js-playground/ui/js/{htmx.js,vxui-ws.js}`（符号链接）
- Create: `examples/run-js-playground/README.md`

- [ ] **Step 1: 写 main.v**

```v
module main

import vxui
import time
import x.json2

// Demonstrates: run_js, run_js_client, js_timeout error handling,
// oob_update console log sync

const default_page_html_file = './ui/index.html'

struct App {
	vxui.Context
mut:
	seq int
}

// param pulls a string parameter from the request
fn param(message map[string]json2.Any, key string) string {
	if params := message['parameters'] {
		if v := params.as_map()[key] {
			return v.str()
		}
	}
	return ''
}

// log_entry builds an OOB console line appended to #console
fn log_entry(ok bool, title string, detail string) string {
	cls := if ok { 'ok' } else { 'err' }
	ts := time.now().format_ss()
	return '<div id="console" hx-swap-oob="beforeend:#console">
		<div class="entry ${cls}">
			<span class="ts">${ts}</span><span class="title">${title}</span>
			<pre>${detail.replace_each(['&', '&amp;', '<', '&lt;', '>', '&gt;'])}</pre>
		</div></div>'
}

// run_and_log executes js on target client and logs the outcome to ALL windows
fn (mut app App) run_and_log(title string, js string, timeout_ms int, cid string) string {
	mut result := ''
	mut ok := true
	if cid != '' {
		result = app.run_js_client(cid, js, timeout_ms) or {
			ok = false
			err.msg()
		}
	} else {
		result = app.run_js(js, timeout_ms) or {
			ok = false
			err.msg()
		}
	}
	app.broadcast(log_entry(ok, title, result)) or {}
	return ''
}

@['/title']
fn (mut app App) do_title(message map[string]json2.Any) string {
	return app.run_and_log('读取页面标题', 'document.title', 3000, param(message, 'cid'))
}

@['/scroll-top']
fn (mut app App) do_scroll(message map[string]json2.Any) string {
	return app.run_and_log('平滑滚顶', 'window.scrollTo({top:0, behavior:\'smooth\'}), \'scrolled\'',
		3000, param(message, 'cid'))
}

@['/toast']
fn (mut app App) do_toast(message map[string]json2.Any) string {
	js := "(()=>{const t=document.createElement('div');t.textContent='来自后端的消息 '+new Date().toLocaleTimeString();t.style.cssText='position:fixed;top:20px;left:50%;transform:translateX(-50%);background:#11998e;color:#fff;padding:10px 18px;border-radius:8px;z-index:99999';document.body.appendChild(t);setTimeout(()=>t.remove(),2500);return 'toast shown'})()"
	return app.run_and_log('弹出 Toast', js, 3000, param(message, 'cid'))
}

@['/accent']
fn (mut app App) do_accent(message map[string]json2.Any) string {
	js := "(document.documentElement.style.setProperty('--accent', ['#38ef7d','#4fc3f7','#ffb74d','#eb3349'][Math.floor(Math.random()*4)]), 'accent changed')"
	return app.run_and_log('随机主题色', js, 3000, param(message, 'cid'))
}

@['/ua']
fn (mut app App) do_ua(message map[string]json2.Any) string {
	return app.run_and_log('获取 UA', 'navigator.userAgent', 3000, param(message, 'cid'))
}

@['/timeout-demo']
fn (mut app App) do_timeout(message map[string]json2.Any) string {
	js := 'const t0 = Date.now(); while (Date.now() - t0 < 8000) {} "done"'
	return app.run_and_log('超时演示（预期失败）', js, 2000, param(message, 'cid'))
}

fn main() {
	mut app := App{}
	app.config.app_name = 'run-js-playground'
	app.config.close_timer_ms = 60000

	vxui.run(mut app, default_page_html_file)!
}
```

- [ ] **Step 2: 写 ui/index.html**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>vxui run_js Playground</title>
  <link rel="stylesheet" href="./stylesheet.css"/>
  <script src="./js/htmx.js"></script>
  <script src="./js/vxui-ws.js"></script>
</head>
<body hx-ext="vxui-ws" style="--accent:#38ef7d">
  <header><h1>run_js Playground</h1>
    <span class="hint">左侧按钮由后端执行 JS 并把结果广播到所有窗口的日志面板</span>
  </header>
  <div class="layout">
    <nav>
      <button hx-post="/title" hx-swap="none">读取页面标题</button>
      <button hx-post="/scroll-top" hx-swap="none">平滑滚顶</button>
      <button hx-post="/toast" hx-swap="none">弹出 Toast</button>
      <button hx-post="/accent" hx-swap="none">随机主题色</button>
      <button hx-post="/ua" hx-swap="none">获取 UA</button>
      <button hx-post="/timeout-demo" hx-swap="none" class="danger">超时演示（预期失败）</button>
      <hr>
      <label>执行目标窗口
        <select name="cid" id="target">
          <option value="">第一个窗口</option>
        </select>
      </label>
      <button id="refresh-targets" class="ghost">刷新目标列表</button>
    </nav>
    <section id="console-wrap">
      <div id="console"></div>
    </section>
  </div>
  <script>
    // 把选中的目标 cid 注入所有按钮请求（hx-vals 动态化）
    document.body.addEventListener('htmx:configRequest', function (e) {
      if (e.detail.path !== '/targets') {
        e.detail.parameters['cid'] = document.getElementById('target').value
      }
    })
    document.getElementById('refresh-targets').addEventListener('click', function () {
      var sel = document.getElementById('target')
      var cur = sel.value
      var ids = (window.vxuiWs && window.vxuiWs.getSocket()) ? [''] : []
      // 框架暂未暴露完整 client 列表给页面；保留“第一个窗口”默认项
      sel.innerHTML = '<option value="">第一个窗口</option>'
      sel.value = cur
    })
  </script>
</body>
</html>
```

说明：页面级"多窗口定向"受限于框架未向页面暴露客户端清单，下拉框当前只提供
"第一个窗口"默认值；`run_js_client` 路径仍由后端保留并在 README 说明。
这是对 spec 中该子项的如实降级，不改框架源码。

- [ ] **Step 3: 写 ui/stylesheet.css**

```css
* { box-sizing: border-box; margin: 0; padding: 0; }
body { background: #12161f; color: #dfe6ee; min-height: 100vh;
	font-family: -apple-system, 'Segoe UI', sans-serif; padding: 20px; }
header h1 { font-size: 22px; }
.hint { color: #8b98a9; font-size: 13px; }
.layout { display: flex; gap: 20px; margin-top: 20px; }
nav { width: 240px; display: flex; flex-direction: column; gap: 8px; }
nav button { text-align: left; padding: 10px 14px; border-radius: 6px;
	border: 1px solid #2a3444; background: #1c2532; color: #dfe6ee; cursor: pointer; }
nav button:hover { border-color: var(--accent); }
nav button.danger { border-color: #eb334955; color: #ff8a97; }
nav button.ghost { background: transparent; color: #8b98a9; }
nav hr { border-color: #2a3444; margin: 8px 0; }
nav label { color: #8b98a9; font-size: 13px; display: flex;
	flex-direction: column; gap: 6px; }
select { background: #1c2532; color: #dfe6ee; border: 1px solid #2a3444;
	border-radius: 6px; padding: 8px; }
#console-wrap { flex: 1; background: #0d1117; border-radius: 10px;
	padding: 12px; height: calc(100vh - 120px); overflow-y: auto; }
.entry { border-left: 3px solid var(--accent); padding: 6px 10px;
	margin-bottom: 8px; background: #161c26; border-radius: 4px; }
.entry.err { border-left-color: #eb3349; }
.entry .ts { color: #8b98a9; font-size: 12px; margin-right: 8px; }
.entry .title { font-weight: 600; }
.entry pre { white-space: pre-wrap; word-break: break-all;
	color: #9fb3c8; margin-top: 4px; font-size: 13px; }
```

- [ ] **Step 4: 写 README.md**

```markdown
# run-js-playground 后端驱动前端

点击按钮 → 后端 `run_js` 执行 JavaScript → 结果经 `broadcast` 同步到所有窗口的日志面板。

## 演示 API

| API | 用途 |
|-----|------|
| `run_js` | 执行 JS 并取回返回值 |
| `run_js_client` | 定向某客户端执行（后端已保留路径） |
| 超时与错误处理 | 「超时演示」按钮触发 `js_timeout` 错误路径 |
| `broadcast` + `oob_update` | 日志面板多窗口同步 |

## 说明

- 页面侧暂无法枚举全部客户端 ID（框架未暴露），下拉框提供「第一个窗口」默认目标；
  `run_js_client` 的定向能力在后端保留。
- 沙箱可用 `app.config.js_sandbox` 配置（禁用模式、结果大小上限、禁止模式列表）。

## 运行

​```bash
v run main.v
​```
```

- [ ] **Step 5: 编译 + 冒烟**

Run: `cd examples/run-js-playground && v -o /dev/null main.v && timeout 8 v run main.v; true`

- [ ] **Step 6: 提交**

```bash
git add examples/run-js-playground/
git commit -m "feat: run_js playground example"
```

---

### Task 6: data-table — 查询管道纯函数（TDD）

**Files:**
- Create: `examples/data-table/main.v`（仅类型与查询函数）
- Test: `examples/data-table/query_test.v`

**Interfaces:**
- Produces（Task 7 复用）:
  - `struct Employee { pub: id int name string dept string salary int hired string status string }`
  - `struct QueryParams { pub: q string dept string sort_col string desc bool page int page_size int }`
  - `apply_query(rows []Employee, p QueryParams) ([][]Employee, int)`
    （返回：分页后的当前页切片们？——否：返回 `(page_rows []Employee, total_filtered int)`；
    此处以精确签名 `apply_query(rows []Employee, p QueryParams) ([]Employee, int)`）

- [ ] **Step 1: 写失败测试**

创建 `examples/data-table/query_test.v`：

```v
module main

fn sample() []Employee {
	return [
		{id: 1, name: 'Alice', dept: '研发', salary: 30000, hired: '2021-01-01', status: '在职'},
		{id: 2, name: 'Bob', dept: '销售', salary: 20000, hired: '2022-02-01', status: '离职'},
		{id: 3, name: ' Carol ', dept: '研发', salary: 35000, hired: '2020-03-01', status: '在职'},
		{id: 4, name: 'Dave', dept: '人事', salary: 25000, hired: '2023-04-01', status: '在职'},
	]
}

fn test_filter_by_q_matches_name_and_dept() {
	rows, _ := apply_query(sample(), QueryParams{q: 'ali', page_size: 10})
	assert rows.len == 1
	assert rows[0].name.trim_space() == 'Alice'
	rows2, _ := apply_query(sample(), QueryParams{q: '研发', page_size: 10})
	assert rows2.len == 2
}

fn test_filter_by_status() {
	rows, _ := apply_query(sample(), QueryParams{status: '离职', page_size: 10})
	assert rows.len == 1
	assert rows[0].id == 2
}

fn test_sort_salary_desc_then_asc() {
	rows, _ := apply_query(sample(), QueryParams{sort_col: 'salary', desc: true, page_size: 10})
	assert rows[0].salary == 35000
	rows2, _ := apply_query(sample(), QueryParams{sort_col: 'salary', page_size: 10})
	assert rows2[0].salary == 20000
}

fn test_pagination_and_total() {
	all := []Employee{cap: 25}
	for i in 1 .. 26 {
		all << Employee{id: i, name: 'E${i}', dept: '研发', salary: i * 100,
			hired: '2020-01-01', status: '在职'}
	}
	page1, total := apply_query(all, QueryParams{page: 1, page_size: 20})
	assert total == 25
	assert page1.len == 20
	assert page1[0].id == 1
	page2, _ := apply_query(all, QueryParams{page: 2, page_size: 20})
	assert page2.len == 5
	assert page2[0].id == 21
}

fn test_default_params() {
	rows, _ := apply_query(sample(), QueryParams{})
	assert rows.len == 4 // 默认按 id 升序、不过滤
}
```

- [ ] **Step 2: 运行确认失败**

Run: `cd examples/data-table && v test .`
Expected: FAIL（`Employee`/`apply_query` 未定义）

- [ ] **Step 3: 最小实现（写入 main.v）**

```v
module main

// Demonstrates: parameters handling, advanced hx-trigger, verb attribute routing

pub struct Employee {
pub:
	id     int
	name   string
	dept   string
	salary int
	hired  string
	status string
}

pub struct QueryParams {
pub:
	q         string
	status    string
	sort_col  string
	desc      bool
	page      int
	page_size int
}

// norm fills defaults for unset fields
fn norm(p QueryParams) QueryParams {
	mut q := p
	if q.page < 1 {
		q.page = 1
	}
	if q.page_size < 1 {
		q.page_size = 20
	}
	if q.sort_col !in ['id', 'name', 'dept', 'salary', 'hired', 'status'] {
		q.sort_col = 'id'
	}
	q.q = q.q.trim_space().to_lower()
	return q
}

// apply_query filters, sorts and paginates. Returns current-page rows and the
// filtered total count.
fn apply_query(rows []Employee, p QueryParams) ([]Employee, int) {
	q := norm(p)
	mut filtered := rows.filter(it.name.to_lower().contains(q.q) || it.dept.contains(q.q))
	if q.status != '' {
		filtered = filtered.filter(it.status == q.status)
	}
	match q.sort_col {
		'salary' { filtered.sort(a.salary < b.salary) }
		'name' { filtered.sort(a.name.to_lower() < b.name.to_lower()) }
		'dept' { filtered.sort(a.dept < b.dept) }
		'hired' { filtered.sort(a.hired < b.hired) }
		'status' { filtered.sort(a.status < b.status) }
		else { filtered.sort(a.id < b.id) }
	}
	if q.desc {
		filtered.reverse_in_place()
	}
	start := (q.page - 1) * q.page_size
	if start >= filtered.len {
		return [], filtered.len
	}
	mut end := start + q.page_size
	if end > filtered.len {
		end = filtered.len
	}
	return filtered[start..end], filtered.len
}
```

- [ ] **Step 4: 运行确认通过**

Run: `cd examples/data-table && v test .`
Expected: PASS（5 个测试）

- [ ] **Step 5: 提交**

```bash
git add examples/data-table/
git commit -m "feat: data-table query pipeline with tests"
```

---

### Task 7: data-table — 服务与前端接线

**Files:**
- Modify: `examples/data-table/main.v`（追加数据生成、路由、渲染）
- Create: `examples/data-table/ui/index.html`
- Create: `examples/data-table/ui/stylesheet.css`
- Create: `examples/data-table/ui/js/{htmx.js,vxui-ws.js}`（符号链接）
- Create: `examples/data-table/README.md`

- [ ] **Step 1: main.v 追加以下内容**

```v
import vxui
import rand
import x.json2
import time

const departments = ['研发', '销售', '人事', '财务', '运营']

const statuses = ['在职', '在职', '在职', '休假', '离职']

// gen_rows deterministically generates n employees
fn gen_rows(n int) []Employee {
	mut rows := []Employee{cap: n}
	names := ['伟','芳','娜','敏','静','强','磊','洋','勇','艳','杰','涛']
	surnames := ['王','李','张','刘','陈','杨','赵','黄']
	for i in 1 .. n + 1 {
		name := surnames[i % surnames.len] + names[(i * 7) % names.len]
		y := 2018 + i % 8
		m := 1 + i % 12
		d := 1 + i % 28
		rows << Employee{
			id:     i
			name:   name
			dept:   departments[i % departments.len]
			salary: 12000 + (i * 937) % 38000
			hired:  '${y}-${m:02}-${d:02}'
			status: statuses[i % statuses.len]
		}
	}
	return rows
}

struct TableApp {
	vxui.Context
mut:
	rows []Employee
}

// qp_from_message parses QueryParams out of request parameters
fn qp_from_message(message map[string]json2.Any) QueryParams {
	mut p := QueryParams{}
	if params := message['parameters'] {
		pm := params.as_map()
		if v := pm['q'] { p.q = v.str() }
		if v := pm['status'] { p.status = v.str() }
		if v := pm['sort'] { p.sort_col = v.str() }
		if v := pm['dir'] { p.desc = v.str() == 'desc' }
		if v := pm['page'] { p.page = v.str().int() }
	}
	return p
}

// render_rows renders tbody content for given page rows plus pager OOB
fn render_table(page_rows []Employee, total int, p QueryParams) string {
	max_page := if total == 0 { 1 } else { (total + p.page_size - 1) / p.page_size }
	mut trs := ''
	for r in page_rows {
		trs += '<tr><td>${r.id}</td><td>${r.name}</td><td>${r.dept}</td>
			<td>${r.salary}</td><td>${r.hired}</td><td><span class="tag ${if r.status == '在职' { 'ok' } else { 'off' }}">${r.status}</span></td></tr>'
	}
	prev_disabled := if p.page <= 1 { ' disabled' } else { '' }
	next_disabled := if p.page >= max_page { ' disabled' } else { '' }
	pager := '<div id="pager" hx-swap-oob="true">
		<button hx-post="/query" hx-vals=\'{"page":${p.page - 1},"q":"${p.q}","status":"${p.status}","sort":"${p.sort_col}","dir":"${if p.desc { 'desc' } else { 'asc' }}"}\'${prev_disabled}>上一页</button>
		<span>第 ${p.page} / ${max_page} 页 · 共 ${total} 条</span>
		<button hx-post="/query" hx-vals=\'{"page":${p.page + 1},"q":"${p.q}","status":"${p.status}","sort":"${p.sort_col}","dir":"${if p.desc { 'desc' } else { 'asc' }}"}\'${next_disabled}>下一页</button>
	</div>'
	return '<tbody id="tbody" hx-swap-oob="innerHTML">#esc_rows</tbody>'.replace('#esc_rows', trs) + pager
}

@['/query']
fn (mut app TableApp) query(message map[string]json2.Any) string {
	p := qp_from_message(message)
	page_rows, total := apply_query(app.rows, p)
	return render_table(page_rows, total, p)
}

@['/reset-sort']
fn (mut app TableApp) reset_sort(message map[string]json2.Any) string {
	p := QueryParams{page: 1}
	page_rows, total := apply_query(app.rows, p)
	return render_table(page_rows, total, p)
}

fn main() {
	rand.seed({
		time.now().unix() as u32  // 占位；如 API 不符改用 rand.seed([u32(time.now().unix())]) 或省略 seed
	})
	mut app := TableApp{}
	app.rows = gen_rows(200)
	app.config.app_name = 'data-table'
	app.config.close_timer_ms = 60000
	vxui.run(mut app, './ui/index.html')!
}
```

实现者注意：`rand.seed` 具体签名以当前 V 版本为准（可能是 `rand.seed([]u32)`），
编译报错则按提示调整或直接删除 seed 行（数据无需真随机）。表格单元格文本均为
程序生成的安全字符，无需转义；若要严格，可复用一个本地 `esc()`。

- [ ] **Step 2: 写 ui/index.html**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <title>vxui Data Table</title>
  <link rel="stylesheet" href="./stylesheet.css"/>
  <script src="./js/htmx.js"></script>
  <script src="./js/vxui-ws.js"></script>
</head>
<body hx-ext="vxui-ws">
  <h1>员工数据表 <small>服务端分页 / 搜索 / 排序</small></h1>
  <div class="toolbar">
    <input type="search" name="q" placeholder="搜索姓名 / 部门…"
           hx-post="/query" hx-trigger="keyup changed delay:300ms"
           hx-include="[name='status'],[name='sort'],[name='dir']" hx-target="#tbody-zone">
    <select name="status" hx-post="/query" hx-trigger="change"
            hx-include="[name='q'],[name='sort'],[name='dir']" hx-target="#tbody-zone">
      <option value="">全部状态</option>
      <option value="在职">在职</option>
      <option value="休假">休假</option>
      <option value="离职">离职</option>
    </select>
    <input type="hidden" name="sort" value="">
    <input type="hidden" name="dir" value="">
  </div>
  <table>
    <thead><tr>
      <th><button hx-post="/query" hx-vals='{"sort":"id"}' hx-include="[name='q'],[name='status'],[name='dir']">ID ▾</button></th>
      <th><button hx-post="/query" hx-vals='{"sort":"name"}' hx-include="[name='q'],[name='status'],[name='dir']">姓名 ▾</button></th>
      <th><button hx-post="/query" hx-vals='{"sort":"dept"}' hx-include="[name='q'],[name='status'],[name='dir']">部门 ▾</button></th>
      <th><button hx-post="/query" hx-vals='{"sort":"salary"}' hx-include="[name='q'],[name='status'],[name='dir']">薪资 ▾</button></th>
      <th><button hx-post="/query" hx-vals='{"sort":"hired"}' hx-include="[name='q'],[name='status'],[name='dir']">入职日期 ▾</button></th>
      <th><button hx-post="/query" hx-vals='{"sort":"status"}' hx-include="[name='q'],[name='status'],[name='dir']">状态 ▾</button></th>
    </tr></thead>
    <tbody id="tbody-zone"><tr><td colspan="6">加载中…</td></tr></tbody>
  </table>
  <div id="pager"></div>
</body>
</html>
```

- [ ] **Step 3: 写 ui/stylesheet.css**（简洁暗色表格样式，列宽/斑马纹/tag 配色）

```css
* { box-sizing: border-box; margin: 0; padding: 0; }
body { background: #12161f; color: #dfe6ee; font-family: -apple-system,'Segoe UI',sans-serif;
	padding: 24px; }
h1 small { color: #8b98a9; font-size: 13px; margin-left: 10px; font-weight: 400; }
.toolbar { display: flex; gap: 10px; margin: 16px 0; }
.toolbar input[type=search], .toolbar select { background: #1c2532; color: #dfe6ee;
	border: 1px solid #2a3444; border-radius: 6px; padding: 9px 12px; min-width: 220px; }
table { width: 100%; border-collapse: collapse; background: #181f2b; border-radius: 10px;
	overflow: hidden; }
th button { background: none; border: 0; color: #8b98a9; cursor: pointer;
	font-size: 13px; padding: 12px; width: 100%; text-align: left; }
th button:hover { color: #dfe6ee; }
td { padding: 10px 12px; border-top: 1px solid #232b38; font-size: 14px; }
tbody tr:nth-child(even) { background: #1c2532; }
.tag { padding: 2px 8px; border-radius: 4px; font-size: 12px; }
.tag.ok { background: #11998e33; color: #38ef7d; }
.tag.off { background: #eb334933; color: #ff8a97; }
#pager { display: flex; align-items: center; gap: 14px; margin-top: 14px; color: #8b98a9; }
#pager button { background: #1c2532; color: #dfe6ee; border: 1px solid #2a3444;
	border-radius: 6px; padding: 8px 16px; cursor: pointer; }
#pager button:disabled { opacity: .4; cursor: default; }
```

- [ ] **Step 4: 写 README.md**

```markdown
# data-table 服务端数据表格

200 行模拟员工数据的无状态查询：搜索（300ms 防抖）、状态下拉、点表头排序、分页。
每次请求携带完整参数，天然支持多窗口一致视图。

## 演示 API

| 能力 | 实现 |
|------|------|
| `hx-trigger` 高级用法 | `keyup changed delay:300ms` 防抖搜索 |
| parameters 处理 | `/query` 解析 q/status/sort/dir/page |
| 动词属性路由 | `@['/query']` POST |

## 运行

​```bash
v run main.v
​```
```

- [ ] **Step 5: 编译 + 冒烟**

Run: `cd examples/data-table && v test . && v -o /dev/null main.v && timeout 8 v run main.v; true`
Expected: 单测通过、启动正常、首屏表格有数据（若首屏空，检查 index.html 是否需要
`hx-trigger="load"` 触发首次 /query——实现时可给搜索框加 `hx-load` 或在 body 上加
`hx-post="/query" hx-trigger="load"` 的隐藏触发器，以实际效果为准）。

- [ ] **Step 6: 提交**

```bash
git add examples/data-table/
git commit -m "feat: server-side data table example"
```

---

### Task 8: 登记文档与整体验证

**Files:**
- Modify: `AGENTS.md`（Examples 目录树与说明区新增四个条目）

- [ ] **Step 1: 更新 AGENTS.md 示例列表**

在项目结构树的 `examples/` 段落追加：

```
    ├── system-monitor/ # Real-time dashboard: broadcast + oob_update + multi-client
    ├── chat/           # Multi-window chat: broadcast + lifecycle events
    ├── run-js-playground/ # Backend-driven frontend: run_js + error paths
    └── data-table/     # Server-side pagination/search/sort patterns
```

并把现有 `file-upload/` 行末尾的注释保持不变（顺序按字母序插入适当位置）。

- [ ] **Step 2: 全量回归**

```bash
v test .
for d in examples/*/; do [ -f "$d/main.v" ] && (cd "$d" && v -o /tmp/opencode/exbin main.v || echo "FAIL $d"); done
```

Expected: 测试全绿；所有示例编译成功。

- [ ] **Step 3: 提交**

```bash
git add AGENTS.md
git commit -m "docs: register new examples in project guide"
```

---

## Self-Review 记录

- Spec 覆盖：4 个示例均有对应 Task；spec 中「run-js 目标选择下拉」因框架未向页面
  暴露客户端列表而如实降级为默认目标（已在 Task 5 与 README 标注），属有意偏差。
- 类型一致性：`apply_query` 最终签名为 `(rows []Employee, p QueryParams) ([]Employee, int)`，
  Task 7 的调用处与此一致；`CpuSnapshot/MemInfo` 仅在 Task 1/2 使用，签名一致。
- 占位扫描：无废弃草稿；
  Task 7 的 `rand.seed` 标注了 API 差异的处理方式。其余步骤均为完整代码。
